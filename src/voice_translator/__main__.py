"""エントリポイント。`python -m voice_translator` で起動する。

現状は GUI 未実装のため起動メッセージのみ。後続ステップで MainWindow を呼び出す。
"""

from __future__ import annotations


def main() -> None:
    """アプリ起動のエントリポイント(暫定)。"""
    print("voice_translator: MVP 構築中。GUI は後続ステップで追加されます。")


if __name__ == "__main__":
    main()
