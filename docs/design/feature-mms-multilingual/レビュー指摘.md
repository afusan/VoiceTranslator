# feature-mms-multilingual 敵対的レビュー指摘

レビュー日: 2026-06-16 / 対象: `master..feature/mms-multilingual`(7 コミット)
レビュー方針: 敵対的(壊れ方・整合崩れ・期待ギャップを探す)。コード/ドキュメントは未修正。

## 総評

機械的に確定した **MMS∩NLLB = 99 言語**の集合は 3 か所(`_MMS_LANGS` /
`CANONICAL_TO_NLLB` / `LANGUAGE_NAMES`)で完全一致(差分ゼロ)を確認。639-1↔639-3 の
往復、Whisper 99 言語の正準化(衝突なし・表示名欠落なし・往復一致)も検証で問題なし。
ASR/Translator/TTS 各 backend の境界変換も一貫している。**致命的バグ・クラッシュは未検出**。
branch 関連 small テスト 233 件 green を再現確認。

以下は「仕様としては妥当だが期待とズレる/堅牢性が薄い」系の指摘。**High はなし**、Medium が
4 件、残りは Low/情報。優先対応の判断材料として整理した。

---

## 検討結果サマリ(2026-06-16、実装者判断)

| 指摘 | 判定 | 一言 |
|---|---|---|
| M-1 主要言語(jpn等)が MMS 非対応 | **修正済** | manual §5-4b に「日本語/中国語/アラビア語/伊語は MMS 非対応 → SAPI/クラウド」を明記 |
| M-2 MPS を無視 | **修正済** | 自前 `_resolve_device` を廃し共有 `resolve_torch_device` に統一(MPS 含む・コードパス1本) |
| M-3 言語ロード失敗が Fatal | **対応しない(ユーザ判断)** | 技術的には要修正評価だが、ユーザ判断により今回は見送り(2026-06-16) |
| M-4 言語失敗で backend 全体を NOT_DOWNLOADED に | **対応しない(ユーザ判断)** | 技術的には要修正評価だが、ユーザ判断により今回は見送り(2026-06-16) |
| L-1 ElevenLabs `fil` vs `tgl` | **修正済** | 当初の共有表別名案は懸念あり→不採用。ElevenLabs 境界で `fil`→`tl`(=`tgl`)へ寄せた |
| L-2 `set_setting` が正準化しない | **要検討** | GUI 経路は常に正準で実害なし。防御として書込み境界の正準化 or 固定テストを検討 |
| L-3 テストが 639-1 コードで stale | **修正済** | LRU テストを 639-3(eng/fra/deu)へ。メソッド名も 639-3 表記へ改名 |
| L-4 large の `sr==16000` 固定 | **不要** | MMS-TTS は全言語 16kHz。問題ない(後述) |
| L-5 クラウド既定言語が 639-1 表記 | **修正済** | google_stt/google_tts の既定・schema を `eng` へ統一(help も 639-3) |
| L-6 同時ロードの重複 DL | **不要** | 既知・意図的(ロック外ロード)。正しさは保たれる(後述) |
| I-1 NLLB 非対応 src の誤認 fallback | **pendList 起票済** | 回帰ではない・入力側の別テーマ。pendList [2026-06-16] に背景込みで記録 |
| I-2 `gen_lang_table` が私的属性依存 | **不要** | 実行時非依存の再生成スクリプト。再生成時に確認すれば足りる |
| I-3 `mms` の既定 config 無し | **不要** | 既存 backend と整合(`_read_str` 既定で吸収) |

→ **修正済 5 件(M-1, M-2, L-1, L-3, L-5)/ 対応しない=ユーザ判断 2 件(M-3, M-4)/ 要検討 2 件(L-2, I-1)/ 不要 4 件(L-4, L-6, I-2, I-3)**。
要修正に挙げた 5 件はすべて対応完了(2026-06-16、全 small テスト緑)。M-3/M-4(言語ロード失敗時の縮退)は
ユーザ判断で見送り。要検討 2 件(L-2 / I-1)は本ブランチ対象外で pendList 候補。

---

## Medium

### [修正済] M-1. MMS は日本語を読み上げられない(本アプリの主用途が成立しない)
MMS∩NLLB の 99 言語に **`jpn` / `zho` / `ara` / `ita` が含まれない**(検証済み。MMS の
チェックポイントに存在しないため)。本アプリは CLAUDE.md / fallback 規則(`jpn > eng`)が
示すとおり**日本語出力が主用途**だが、TTS に `mms` を選ぶと:
- 出力言語候補(`restrict_to_tts(NLLB, MMS)`)から `jpn` が消える。
- 現在値 `jpn` は `compute_tgt_selection` で `eng` へ自動 fallback(バナーは出る)。

つまり「日本語で喋らせたいユーザが MMS を選ぶと、黙って英語出力に化ける」。fallback 機構
自体は機能するが、**`docs/manual.md §5-4b` はこの制限(主要言語が抜ける)を書いていない**。
スワヒリ等を読み上げる用途には正しい backend だが、「日本語/中国語/アラビア語は MMS 非対応 →
これらの出力には SAPI/Piper/クラウドを使う」を manual に明示すべき。
→ 仕様は妥当。**ドキュメントの期待ギャップ**として要追記。
→ **検討結果【修正済 2026-06-16】**: 挙動(候補から jpn が消え、現在値は eng へ fallback +バナー)は
  設計どおり正しい。manual §5-4b に「日本語(jpn)・中国語(zho)・アラビア語(ara)・イタリア語(ita)は
  MMS 非対応 → SAPI / クラウド TTS を使う。日本語用途では mms 以外を選ぶ」を明記した(コード修正なし)。

### [修正済] M-2. MMS の device 解決が MPS(Apple Silicon)を無視
`MmsTtsBackend._resolve_device()`(`tts/mms_backend.py:120-129`)は `cpu`/`cuda` しか返さず、
`torch.cuda.is_available()` が偽なら無条件 `cpu`。共有ヘルパ `common/device.py:resolve_torch_device`
は **mps を auto 解決する**のに、MMS は同じ torch 系なのに自前実装で MPS を落としている。
- 配布方針(CLAUDE.md「配布方針」: GPU/アクセラレータがあれば自動使用=**Apple Silicon MPS 含む**、
  「device 切替はコードパス1本」)に反する。
- `device="mps"` を**明示**しても `_resolve_device` の分岐(`in ("cpu","cuda")`)を外れ、cpu に落ちる。
→ NLLB と同じく `resolve_torch_device` を使えば一掃できる。VITS×MPS の実機懸念があるなら、
   その判断(なぜ自前で MPS を外したか)を docstring に残すべき(現状は理由の記載なし)。
→ **検討結果【修正済 2026-06-16】**: 自前 `_resolve_device` を廃し、共有 `resolve_torch_device` に統一
  (auto→cuda→mps→cpu、明示指定はそのまま通す)。配布方針「device 切替はコードパス1本」に揃った。
  MPS で VITS が動かない実機事例が出たら、その時点で理由を docstring に残して分岐を足す方針。

### [対応しない(ユーザ判断)] M-3. 言語パックのロード失敗が `FatalError` でパイプライン全停止
`_load_voice`(`tts/mms_backend.py:171-204`)は DL/初期化失敗時に **`FatalError`** を投げる。
MMS の言語ロードは**会話中(初回発話)に起こり得る**(prefetch されなかった/別言語へ切替直後)。
- 一過性のネットワーク断で 150MB の DL が落ちると、`FatalError` → パイプライン即停止。
- 起動時 1 回ロードの NLLB が Fatal なのは妥当だが、MMS は**実行中の動的ロード**なので、
  `RecoverableError`(リトライ)か `SkipError`(当該発話だけ捨てる)の方が縮退として自然。
→ 「prefetch が裏で確保するから実害は小さい」という前提だが、prefetch 失敗時の本線が Fatal。
→ **検討結果【対応しない(ユーザ判断 2026-06-16)】**: 技術的には要修正と評価(`synthesize` 経路を
  SkipError 縮退にし、一過性ネット断でセッションごと停止するのを防ぐべき)。ただしユーザ判断により
  今回は対応しない。現状は「初回 DL 失敗時はパイプライン停止 → 再度『開始』」で運用する。再着手時は
  M-4 とセットで `_load_voice` の失敗ハンドラを直す(縮退方針は本ファイルの議論メモ参照)。

### [対応しない(ユーザ判断)] M-4. 言語ロード失敗で backend 全体ステータスを `NOT_DOWNLOADED` に汚す
`_load_voice` の失敗ハンドラは `self._set_status(ModelStatus.NOT_DOWNLOADED)` を呼ぶ
(`tts/mms_backend.py:190, 203`)。MMS はエンジン準備済み(LOADED)で**個別言語**を足す設計
なのに、1 言語のロード失敗で backend 全体が「未 DL」表示になる。
- キャッシュ済みで動作可能な言語があっても UI 上は壊れて見える。成功ロードまで回復しない。
- **背景 prefetch の失敗**(AppController は例外を握る)でも、backend 内部でステータスが先に
  `NOT_DOWNLOADED` に倒れた状態が残る。→ ユーザ操作と無関係にステータスが汚れる。
→ 言語単位の失敗はレイヤ全体状態に波及させない設計が望ましい(集約テキストへ言語名併記で代替、
   は Plan の当初方針とも整合)。
→ **検討結果【対応しない(ユーザ判断 2026-06-16)】**: 技術的には要修正と評価(エンジンは LOADED の
  まま当該言語だけ失敗させるべき)。ただしユーザ判断により今回は対応しない。M-3 の議論から派生した
  項目だが、本ブランチでは見送る。再着手するなら M-3 とセットで `_load_voice` の失敗ハンドラを直す。

---

## Low

### [要修正(軽)] L-1. ElevenLabs の Tagalog 申告コード不整合(`fil` vs `tgl`)
ElevenLabs は Tagalog を `"fil"` で申告(`iso1_to_iso3("fil")` は未知 passthrough → `"fil"`)。
NLLB/MMS/Whisper は同言語を `"tgl"` で申告。AND(`restrict_to_tts`)で一致せず、
**ElevenLabs+NLLB のとき Tagalog が出力候補から落ちる**。ElevenLabs は voice 主体で実合成は
コードに依存しないため実害は「候補欠落」のみ。**移行前(`fil` vs `tl`)からの持ち越し**だが、
今回の正準化で `tgl` に揃った他 backend との差として残った。`ISO1_TO_ISO3` に `"fil"`→`"tgl"`
等の別名吸収を入れるか、ElevenLabs 側を `"tgl"` 申告にすれば解消。
→ **検討結果【修正済(2026-06-16)】**: 当初案(`ISO1_TO_ISO3` に `fil→tgl` を足す)は**懸念ありで不採用**。
  理由: (1) `fil`(Filipino)と `tgl`(Tagalog)は ISO 上**別コード**で機械的同一視は不正確、(2) `fil` は
  639-1 でないため「639-1→639-3」表の契約を壊し、`iso1_to_iso3` を使う**全 backend にグローバルに効く**
  判断を共有表へ隠してしまう。
  → 採用した修正: **ElevenLabs backend の境界**で吸収。対応リストの `"fil"` を `"tl"`(Tagalog の 639-1)
  に置換し `iso1_to_iso3("tl")="tgl"` で正準 `tgl` へ寄せた。共有テーブルは無変更。ElevenLabs の合成は
  voice_id 主体でコード非依存なので安全(fil/tgl の差は実合成に影響しない)。テストも `tgl` 在/`fil` 不在へ更新。

### [要検討] L-2. `set_setting` は言語コードを正準化しない
正準化(`_normalize_language_codes`)は **`ConfigStore.load()` 時のみ**。`set_setting("languages",
"tgt", X)` は X をそのまま保存する。GUI 経路(プルダウン/🔍)は常に正準 639-3 を渡すので
実害はないが、プログラム経由や旧値の直接書込みで 639-1("ja")が入ると、次回 load まで
正準化されず、その間 **MMS の prefetch は no-op / synthesize は SkipError** になる。
防御の薄さ(正準化を書込み境界にも置くか、最低限テストで「set→get が正準」を固定)。
→ **検討結果【要検討】**: GUI(プルダウン/🔍)は常に正準を渡すので現状実害なし。`set_setting` 全体に
  正準化を挟むと「設定の保存はそのまま」という単純さが崩れる(言語キーだけ特別扱いになる)ため、
  入れるなら languages キー限定 + 固定テスト。優先度は低。本ブランチでは見送り、pendList 行き候補。

### [修正済] L-3. テストが 639-1 コードで書かれている(移行後の実コードと乖離)
`tests/test_mms_backend.py` の LRU/キャッシュ系が `"en"/"fr"/"de"`(639-1)で検証。
`_ensure_language` はメンバーシップ非検証(`_load_voice` がゲート)のためモック差替えで通るが、
**本番が渡すのは 639-3**(`"eng"` 等)。テストが移行後の実コードパスを表していない。
`test_returns_sorted_iso639_1_subset` の名称・docstring も 639-1 のまま stale。
→ **検討結果【修正済 2026-06-16】**: LRU/キャッシュテストの `"en"/"fr"/"de"` を `"eng"/"fra"/"deu"` へ
  更新し、`test_returns_sorted_iso639_1_subset` を `test_returns_sorted_canonical_iso639_3` へ改名
  (+ 全コードが 3 文字以上=639-3 であるアサートを追加)。挙動は不変。

### [不要] L-4. large テストの samplerate ハードコード
`test_mms_tts_large.py` が `sr == 16000` を固定アサート。MMS は多くが 16kHz だが普遍ではない。
eng/swh では正しいが、対象言語を増やすと脆い。
→ **検討結果【不要】**: MMS-TTS は全言語 16kHz 固定(VITS 構成)なので、現対象集合では `sr==16000` は
  正しく、脆くない。気になるなら `sr > 0` に緩める選択肢はあるが、large テストは「実際に 16kHz が
  返ること」を固定する意味もあり、必須の修正ではないと判断。

### [修正済] L-5. クラウド backend の既定言語が 639-1 表記のまま
`backend_setup.py`(google_stt=`"en"` L425 / google_tts=`"en"` L664)・
`layer_settings_schema.py`(L432, L585=`"en"`)。各 backend の `__init__` が
`iso1_to_iso3` で正準化するため動作はするが、**内部標準 639-3 と既定値の表記が不一致**。
schema 既定は `"eng"` が一貫(GUI 編集時に 639-1 を見せない)。
→ **検討結果【修正済 2026-06-16】**: `backend_setup.py`(google_stt / google_tts)と
  `layer_settings_schema.py`(両 default_language)の既定を `"eng"` に統一し、help_text の表記も
  「ISO 639-3」へ修正。動作は不変(backend 側で正準化)だが、内部標準と既定値表記が揃った。

### [不要] L-6. 同一言語の同時ロードで重複 DL/構築
`_ensure_language` はロック外でロード(意図的=重い DL 中に他スレッドを止めない)。
同一言語を prefetch と synthesize が同時要求すると **150MB の DL/構築が二重**に走り、
後勝ちで一方を破棄。コメントで認知済み・正しさは保たれるが、稀に無駄が出る。
→ **検討結果【不要】**: 既知・意図的なトレードオフ(重い DL 中に他スレッドを止めない設計)。正しさは
  保たれ、二重 DL も HF キャッシュ済みなら 2 回目は実質コピー。prefetch を選択契機で先行させる配線で
  実発生は稀。最適化の価値が低く現状維持。

---

## 情報(対応不要・把握のみ)

- **【要検討(将来)】I-1**: Whisper 対応だが NLLB 非対応の 14 言語(`afr, bre, haw, lat, lin, ltz, mlg, mri, nob,
  oci, san, snd, tuk, yid`)を src に選ぶと、NLLB の src コードが `eng_Latn` に fallback し
  **源言語を誤認した翻訳**になる。既存挙動(NLLB が元から非対応)で 639-3 化による回帰ではない。
  `ISO_TO_NLLB` 拡充 or src 候補を「翻訳もできる言語」に絞る将来課題。
  → **検討結果【pendList 起票済(2026-06-16)】**: 639-3 化による回帰ではなく既存挙動(NLLB が元から
    非対応の src を eng_Latn に倒す)。本ブランチ(出力側 MMS 多言語化)の対象外=入力側の ASR↔翻訳
    カバレッジ別テーマ。実コードで該当 13 言語を算出し、メカニズム・対応案(申告表拡充 / 入力側警告)・
    再検討トリガを `docs/design/pendList.md` [2026-06-16] に記録した。
- **【不要】I-2**: `gen_lang_table.py` が `tokenizer._extra_special_tokens`(私的属性)に依存。再生成
  スクリプト(実行時非依存)だが、transformers 版更新で壊れ得る。再生成時に要確認。
  → **検討結果【不要】**: 本番コードは依存しない再生成用スクリプト。年に数回の言語表更新時にしか走らず、
    壊れてもその場で気づく。出力(各ソースの dict)は固定値として commit 済みなので実行時影響ゼロ。
- **【不要】I-3**: `backends_config` に `mms` の既定エントリなし(piper/elevenlabs と同様 `_read_str`
  の default で吸収)。`auto_load` 既定が無く常に手動ロード=既存 backend と整合。Plan Phase4 の
  「既定値補完」チェックは `_read_str` 依存で済ませた、で整合と判断。
  → **検討結果【不要】**: 既存 TTS backend(piper/elevenlabs)と同じ扱いで一貫。`_read_str` の既定で
    吸収され、明示エントリは不要。指摘自体が「整合」と結論しており同意。

---

## 検証メモ(再現用)

- 集合整合: `_MMS_LANGS`(99)= `CANONICAL_TO_NLLB` keys(99)= `LANGUAGE_NAMES` 該当分。差分 0。
- AND: `MmsTtsBackend.supported_output_languages()` ∩ `Nllb200...supported_target_languages()` = 99。
- 639-1↔639-3 往復: `ISO1_TO_ISO3` / `ISO3_TO_ISO1` 不整合 0。Whisper 99 語の正準化で衝突 0・
  表示名欠落 0・`iso3_to_iso1(iso1_to_iso3(c))==c` 全一致。
- 主要言語 MMS 可否: jpn=✗ / zho=✗ / ara=✗ / ita=✗ / eng=○ / kor=○ / spa,fra,deu,hin,rus,por,
  vie,tha,ind,tur=○。
- テスト: branch 関連 8 ファイル 233 件 green(`test_mms_backend` / `test_languages` /
  `test_logic_language_filter` / `test_multilingual_integration` / `test_logic_language_choices` /
  `test_config_store` / `test_settings_panel_lang` / `test_app_controller`)。
